# Core pipeline (simulation backend) — CPU only, no GPU, no API keys.
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# Optional, only for real open-model backends:
#   --backend hf     ->  pip install torch transformers accelerate
#   --backend ollama ->  install Ollama (https://ollama.com) and `ollama serve`
# transformers>=4.44
# torch>=2.2
# accelerate>=0.33
